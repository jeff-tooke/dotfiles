sourceDir = "{{ .chezmoi.homeDir }}/.dotfiles"

# Data section
[data]
hostname = "{{ .chezmoi.hostname }}"

work_email = "{{ (onepasswordDetailsFields "qllmussk6lf2xuqzx35auu6gfe").username.value }}"
personal_email = "{{ onepasswordRead "op://Personal/Github/email" }}"

wifi_ssid = "{{ (onepasswordDetailsFields "wcajdpagrzdqterfeqjxmpat4u").username.value }}"
