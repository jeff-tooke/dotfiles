[mgr]
sort_by        = "natural"
show_hidden    = true
scrolloff      = 10
title_format   = "Yazi: {cwd}"

[opener]
edit = [
	{ run = '${EDITOR:-nvim} "$@"', desc = "$EDITOR", block = true, for = "unix" },
]
